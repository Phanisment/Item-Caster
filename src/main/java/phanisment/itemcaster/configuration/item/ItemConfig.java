package phanisment.itemcaster.configuration.item;

import org.bukkit.inventory.ItemStack;

import phanisment.itemcaster.Main;

import java.io.File;
import java.util.HashMap;
import java.util.Map;

public class ItemConfig {
/*
	private final Map<String, ItemStack> caches = new HashMap<>();
	private final Main plugin;
	
	public ItemConfig(Main plugin) {
		this.plugin = plugin;
		
		File itemFolder = new File(plugin.getDataFolder(), "item");
		if(!itemFolder.exists()) {
			itemFolder.mkdirs();
		}
		
		File[] files = itemFolder.listFiles();
		if (files != null) {
			for (File file : files) {
				if (file.isFile()) {
					
				}
			}
		}
	}
*/
}